boolean running = false;
    while (running) {
        // TODO do your stuff
        try {
            Thread.sleep(1000);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }