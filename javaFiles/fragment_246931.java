while (ch.hasNext()) {
        String str = ch.next();
        for(int i = 0; i<str.length(); i++) {
            if(str.charAt(i) == '$') {
                //do something
            }
        }
    }