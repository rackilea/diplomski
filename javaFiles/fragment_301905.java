else {
    for (int i = 0; i < b.length; i++) {
        if (a[i].length > 0) {  // add check for empty array
            for (int j = 0; j < b.length; j++) {
                //...
            }
        }
    }
}