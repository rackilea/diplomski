public String toString() {
    return String.format("Id: %d%n" + //
            "Name (lname, fname): %s, %s%n" + //
            "Address: %s%n" + //
            "Age: %d",//
            id, lname, fname, address, age//
            );
}