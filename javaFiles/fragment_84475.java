for(int i = 0; i < rows; i++)
{
    for(int j = 0; j < cols - 1; j++)
    {
        if(array[i][j] + array[i][j + 1] == sumToFind)
            //Do Something, since your question doesn't mention how you want
            //the data to be put in the new array
    }

}