for (int j = 3;j <= 80; j+=4){
  if (CarsLv1.cars[j] == 0) {
    switch(j){
      case 3:
        // Whatever.
        break;  // This breaks the case.
    }
    break;  // This breaks the loop;
  }
}