while (true) {
    // Read a line
    String line = in.readLine();
    if (line == null) {
        // Abort, nothing to read anymore
        break;
    }

    // Print what we have read
    System.out.println(line);
}