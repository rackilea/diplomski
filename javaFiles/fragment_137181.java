serverSocket = new ServerSocket(serverPort);
    try {
        File incomingFile = new File("testfile.txt");
        while (true) {
            // your code
        }
    } finally {
        serverSocket.close();
    }