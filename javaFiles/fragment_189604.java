if (element!= null) {
    int indexOf = -1;
    for (int index = 0; index < arrays.length; index++) {
        if (arrays[index].equals(element)) {
            indexOf = index;
            break;
        }
    }