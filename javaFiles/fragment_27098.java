while (input != -99) 
      { 
            if (input > MAX_VALUE) 
            { 
                 MAX_VALUE = input; 
            } 
            else if (input < MIN_VALUE) 
            { 
                 MIN_VALUE = input; 
            }
      }