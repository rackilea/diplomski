if(message.getPayload().getBody().size() > 0) {
        // do your work with:
        // message.getPayload().getBody()
    } else {
        // do your work with:
        //message.getPayload().getParts()
    }