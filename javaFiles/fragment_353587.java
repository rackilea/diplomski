public String officeLoc () {
    if (...) {
        String tempLoc = ...;
        return tempLoc; // <- no problem
    } else {
        String tempLoc = ...;
        return tempLoc; // <- no problem
    }
}