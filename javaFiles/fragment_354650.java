PokemonObject po = null;
if (some test) {
    po = new PokemonObject();
    // ... use po
} else if (another test) {
    po = new PokemonObject();
    // ... use po
} else if (a third test) {
    po = new PokemonObject();
    // ... use po
}
if (po != null) {
    // try to use po (works!)
}