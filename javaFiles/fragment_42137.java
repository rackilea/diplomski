man.firstSection();
if (!man.isCorrect())
    return 0; // close the function but not the program.

man.secondSection();
if (!man.isCorrect())
    return 0; // close the function but not the program.
....