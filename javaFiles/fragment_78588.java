else if(questions.size()-1 > count[0])
 {
        count[0]++;
                 mQuestion.setText(questions.get(count[0]).question); // you dont need calculate the module anymore
                        pwindo.dismiss();
 }else
      // you can restart again or finish