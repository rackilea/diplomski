TextButton textButton = null;

    //...

    ( (Texture) textButton.getStyle().down ).setFilter(TextureFilter.Nearest, TextureFilter.Nearest);
    //there are also: up, disabled, check, checkedOver, over Drawables