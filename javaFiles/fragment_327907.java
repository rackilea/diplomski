int r=1;
for (OrderEntity orderEntity : excelList) {
    Row row=Sheet.createRow(r++);
    // more code
    r++;
    // more code
    r++;
    // more code
    r++;
    // more code
    r++;
    // more code
    r++;
}