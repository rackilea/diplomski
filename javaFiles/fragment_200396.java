@Override
public final Object receiveEvent() throws Exception {
    Server f = new Server();

    switch (f.getInf()) {
    case CX_CLIENT:
        // set
        break; // optional

    case CX_SERVER:
        // set
        break; // optional

    default:
        // some code
    }

}