String choice;
        switch (choose) {
            case 1:  choice = "start again ";
                     break;
            case 2:  choice = "exit";
                     break;

            default: choice = "Invalid match";
                     break;
        }