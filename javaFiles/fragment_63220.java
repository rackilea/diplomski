for(int k=0;k<200;k++)
        {
             if(a[k]==0) //For assigning value in the last space of the array
             { 
                 a[k]=i;
                 a[k+1]=0;
             }
        }