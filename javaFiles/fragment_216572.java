public static int countEvenWord(String name) { 
Set<String> set = new HashSet<>();

    int numberEven = 0;
    if(name.length() % 2 == 0) {
        set.add(name);
        }
    }
    return set.size();
}