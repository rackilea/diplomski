public class Player {
      private String name;
      private int id;
      // add constructor, getters, setters
  }

  Map<String, Object> player = new HashMap<>();
  player.put("name", "fred");
  player.put("id", 42);