ImageButton b1;

@Override protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    setContentView(R.layout.layout);
    // now assign your view references
    b1 = (ImageButton) findViewById(R.id.b1);
    ...
}