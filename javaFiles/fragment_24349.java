int option;  // created outside the loop
do {
    System.out.print(menu);
    option = in.nextInt();  // used inside the loop
    switch(option)
    {
        // CASES AND CODE
    }
} while(option != 0);