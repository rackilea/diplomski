public class LoginPage extends View {

     public LoginPage() {
         VBox login = new VBox();
         ... 
         setCenter(login);
         ...
         VBox newAccount = new VBox();
         ...
         setBottom(newAccount);
     }
}