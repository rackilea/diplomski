private void parseData(JSONArray array) {

    // Your code for parsing

    // Notify the adapter that data has changed and the Adapter will populate RecyclerView accordingly.
    adapter.notifyDataSetChanged();
}