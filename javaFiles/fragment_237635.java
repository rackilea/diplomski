function main{
    // stuff going on
    array result;
    result = assemble (a, b c);
}

function array assemble (array a, array b, array c){
    // do something with the arrays
    // assing result to d
    return array d
}