...
if (isCellSelected(index_row, index_col)){
    comp.setBackground(Color.GRAY);  
} else {
    if (index_col == 34) {
        comp.setBackground(Color.GRAY);                   
    } else {
        comp.setBackground(Color.WHITE);
    }
}
...