for ( int i = 0; i < 300; ++i ) {
    for ( int j = 0; j < 300; ++j ) {
        if ( array[i][j] == 255 ) {
            // Found the correct i,j - print them or return them or whatever
        } 
    }
}