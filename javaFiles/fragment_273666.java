// First iteration 
 if (score > topSScore) {  // topSScore = 0 
            topSName = s;
            topSScore = score;
        } else if (score > secondSScore) { //secondSScore = 0
            secondSName = s;
            secondSScore = score;
        }