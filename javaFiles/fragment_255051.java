<%

    List list = fetchyourlisthere();

    // check its size

    if(i<list.size()) {

      list.get(i);

    // this will return you the object for that particular question.

     // now increment i

     i++;
    }

    %>