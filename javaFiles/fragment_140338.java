if(one.charAt(i) == two.charAt(j))
            {
               cnt = 1;     // ont for character equal
            }

        }
if(cnt == 0)
fin+=one.charAt(i);
    }

    System.out.print(" Ret ::"+fin);